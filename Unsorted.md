#Unsorted list of new things and changes

They are moved to the other files when they are explained.

* compaction of views and views cleanup
* changes feed on views (experimental)
* update sequence is now an opaque string
* All the things in _node, like _node/nodename@nodeip/_config
* Bulk get
* Global changes
